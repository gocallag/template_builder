# Oracle Linux Systems
- name: (PATCHING-PREPARE, oracle) Oracle Linux-specific preparation tasks
  when: ansible_facts['os_family'] | lower == "redhat" and ansible_facts['distribution'] | lower == "oraclelinux"
  block:
    - name: (PATCHING-PREPARE, oracle) Placeholder for Oracle Linux-specific preparation tasks
      ansible.builtin.debug:
        msg: "No preparation tasks defined for Oracle Linux systems yet."
